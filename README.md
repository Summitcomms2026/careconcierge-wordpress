# CareConcierge WordPress Theme

Custom WordPress block theme for CareConcierge Health.

This repository tracks only the custom theme, not WordPress core, uploads, plugins, cache files, or LocalWP configuration.
